---
id: AFM-0007
state: published
title: "Agent deletes local files after misinterpreting a command sequence (Gemini CLI)"
one_liner: "Asked to rename a directory and move its contents, the agent's mkdir silently failed; a wildcard move against the non-existent target overwrote every file onto the same name until all but the last were destroyed."
class: local-file-deletion
surface: shell
verb: overwrite
agent: [gemini-cli]
recoverability: reversible
blast_radius: project
detection_latency: immediate
salvager_response: recover
intent: accident
autonomy_level: L2-on-loop
incident_date: "2025-07"
xrefs:
  - source_name: aiid
    external_id: "1178"
    url: "https://incidentdatabase.ai/cite/1178/"
  - source_name: cwe
    external_id: "CWE-754"
    url: "https://cwe.mitre.org/data/definitions/754.html"
sources:
  - name: "AI Incident Database — Incident 1178"
    url: "https://incidentdatabase.ai/cite/1178/"
    tags: [incident-report]
  - name: "gemini-cli issue #4586 (first-hand report)"
    url: "https://github.com/google-gemini/gemini-cli/issues/4586"
  - name: "WinBuzzer coverage"
    url: "https://winbuzzer.com/2025/07/26/googles-gemini-cli-deletes-user-files-confesses-catastrophic-failure-xcxwbn/"
tags: [gemini-cli, data-loss, read-after-write]
---

## What happened
Gemini CLI, acting as a coding agent on the user's real filesystem (Windows) with shell
access, was asked to rename a folder and move its contents into a new subfolder. Its `mkdir`
for the destination **failed**, but the agent continued as if it had succeeded, then issued
a wildcard `move` at the non-existent target. On Windows, `move *` to a missing destination
renames each file to the same name in sequence, overwriting the previous one each step —
destroying every file but the last. The agent called the result "unacceptable and
irreversible."

## Why it passed the checks
**No read-after-write verification.** The agent never read filesystem state (`dir`) after a
destructive step to confirm the world matched its assumptions; it assumed the `mkdir`
succeeded and built on that false premise (CWE-754, improper check for unusual conditions).

## What Salvager does about this class
This is the center of Salvager's promise: local, uncommitted files — exactly what the
watcher captures and git does not. If the watcher was running, each overwritten file has its
prior revision in the store, and `salvager restore-at <ms>` returns the whole set to the
instant before the `move` cascade, in one step. The agent called the damage "irreversible"
because it measured the world with the wrong safety net (git). With a per-file net, it was
not.
